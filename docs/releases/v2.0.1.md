# Pronunciation Mapper V2.0.1 릴리스 기록

- 상태: Release candidate
- 준비일: 2026-07-17
- 패키지 버전: `2.0.1`
- 기준 버전: [v2.0.0](https://github.com/hyeonsangjeon/pronunciation-mapper/releases/tag/v2.0.0)
- PyPI project: [pronunciation-mapper](https://pypi.org/project/pronunciation-mapper/)

## 릴리스 목적

V2.0.0의 Foundry-first bounded agent 구조와 공개 API는 유지하면서, 최종 소스 감사에서 발견한 숫자/조사 correctness 문제와 Ollama 생성 상한을 보완합니다. 공개 PyPI 사용자가 설치·실행·결과를 더 빨리 확인할 수 있도록 README와 source distribution도 함께 정리합니다.

## 수정 범위

### Correctness

- `customer만`, `"customer"만`, `C++만`, `account이`, `계정이`처럼 canonical term이나 일반 명사에 붙은 한국어 조사를 숫자로 오인하지 않습니다.
- `커스터머만 → customer만` 결과에 mapper를 다시 적용해도 같은 문자열을 유지합니다.
- 호출별 `threshold`는 생성자와 동일하게 finite, non-bool, `0..1` 값만 허용합니다.

### Provider guardrail

- Ollama structured-output 요청에 기본 `max_output_tokens=2048`을 추가하고 `num_predict`로 전달합니다.
- Qwen 계열을 포함한 reasoning model이 JSON 대신 thinking budget을 소비하지 않도록 `think=False`를 전달합니다.
- Foundry/Ollama extra 미설치 오류는 PyPI package 설치 명령을 안내합니다.

### Packaging과 문서

- sdist에 eval runner, golden cases, vocabulary, tests, docs와 운영 workflow를 포함합니다.
- 게시 workflow가 annotated tag, `pyproject.toml` version과 runtime `__version__`을 함께 검증합니다.
- README에 provider 비교표, credential이 필요 없는 입력·출력 예제와 PyPI 월간 download badge를 추가합니다.
- download badge는 PSF 공개 데이터 기반 pypistats가 제공하며 알려진 mirror를 제외하고 하루 한 번 갱신됩니다.

## 검증 증빙

| 구분 | 결과 |
| --- | --- |
| 전체 test suite | 67 passed, 1 live test skipped, 105 subtests passed |
| Ruff | repository 전체 lint 성공 |
| Bandit | package source medium/high finding 없음 |
| Dependency audit | Foundry/Ollama 포함 runtime dependency 알려진 취약점 없음 |
| Offline golden set | 15/15 exact match, false rewrite rate 0.0 |
| Package | wheel·sdist build 및 metadata check 성공 |
| Fresh install | wheel·sdist 각각 새 환경에서 import, CLI, mapping과 test 성공 |
| Documentation | Markdown/local link, Pages asset와 JavaScript syntax 검증 성공 |

Foundry live test는 V2.0.0에서 external tenant 실제 deployment로 이미 통과했고, 이번 patch는 Foundry request 계약을 변경하지 않습니다. credential이 필요한 live test 한 건은 기본 offline suite에서 명시적으로 skip합니다.

## 릴리스 체크리스트

- [x] 소스·provider·패키징 독립 감사
- [x] 숫자/조사와 threshold 회귀 테스트
- [x] Ollama output 상한 테스트
- [x] README와 다운로드 카운터 정리
- [x] sdist 독립 테스트
- [ ] release PR merge와 main CI
- [ ] `v2.0.1` annotated tag와 GitHub Release
- [ ] PyPI Trusted Publishing
- [ ] PyPI attestation과 fresh-install 재검증

게시 후 GitHub Release, workflow run, PyPI file digest와 최종 설치 검증 결과를 이 문서에 추가합니다.
